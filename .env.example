# Optional: Path to Homebrew binaries (needed for cron/launchd which don't have full PATH)
# HOMEBREW_PATH=/opt/homebrew/bin

# Optional: User home directory (needed for Claude to find its config at ~/.claude/)
# USER_HOME=/Users/yourname

# Optional: Path to Claude CLI binary (defaults to "claude" in PATH)
# CLAUDE_PATH=/usr/local/bin/claude
